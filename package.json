{
  "name": "phoenix-codex-frontend",
  "version": "1.0.0",
  "description": "Phoenix Codex storefront — static HTML/CSS/vanilla JS, deployed on Vercel. The API lives in the separate codex-backend repo.",
  "private": true,
  "license": "ISC",
  "scripts": {
    "start": "npx --yes serve ."
  }
}
